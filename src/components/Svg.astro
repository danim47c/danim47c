---
import { readFile } from "fs/promises";

interface Props {
  src: string
}

const route = Astro.props

const svg = await readFile(route.src, "utf8")
---

<svg set:html={svg}/>

<style lang="scss">
  svg {
    width: 100%;
    height: 100%;
  }
</style>