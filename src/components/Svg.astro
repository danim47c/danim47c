---
interface Props {
  src: string;
}

const route = Astro.props;

console.log(`route.src`, route.src);

const svg = await import(`../../${route.src}?raw`);

console.log({route, svg})

---

<Fragment set:html={svg.default} />

<style lang="scss">
  svg {
    width: 100%;
    height: 100%;
  }
</style>
