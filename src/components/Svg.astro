---
interface Props {
  src: string;
}

const route = Astro.props;

const svg = await import(`${route.src}?reaw`);
---

<Fragment set:html={svg} />

<style lang="scss">
  svg {
    width: 100%;
    height: 100%;
  }
</style>
